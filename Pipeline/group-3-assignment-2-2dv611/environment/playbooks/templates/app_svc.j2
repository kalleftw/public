{#..#}
---
apiVersion: v1
kind: Service
metadata:
  name: '{{ app }}'
spec:
  selector:
    app: '{{ app }}'
  ports:
  - protocol: TCP
    port: {{ exposed_port }}
    targetPort: {{ container_port }}