{#..#}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ app }}-proxy
spec:
  type: NodePort
  selector:
    app: {{ app }}-proxy
  ports:
    - protocol: TCP
      port: {{ exposed_port }}
      nodePort: {{ node_port }}